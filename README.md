# YoutubeTrends
A modular Python analytics pipeline for processing YouTube trending video data. The project loads raw CSV files, cleans and combines them, generates insights, creates visualizations, and produces an automated report.
